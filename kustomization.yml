apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: weather-app
  namespace: flux-system1
spec:
  interval: 5m
  path: ./kustomize
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-source
  targetNamespace: default
  timeout: 3m
  wait: true

